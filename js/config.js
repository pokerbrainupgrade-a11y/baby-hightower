// The one place dates live. If the dating ultrasound moves the due date,
// change these two and the whole app (counter, trimesters, countdown) follows.
export const LMP = '2026-08-03';      // last menstrual period (confirmed)
export const DUE = '2027-05-11';      // due date — May 11, 2027
export const TZ = 'America/Phoenix';  // all "today" math uses this zone (no DST)

export const APP_VERSION = '1.2.0';   // keep in step with VERSION in sw.js
export const USERS = ['Q', 'Staci'];
export const FIREBASE_SDK = 'https://www.gstatic.com/firebasejs/12.4.0';
